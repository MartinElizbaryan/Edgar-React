import sum, { minus } from "./main";
import { sum as suuuuum, multiple } from "./fn2";

const App = () => {

  return (
    <div>
      <h1>Count: {result}</h1>
    </div>
  );
};

export default App;
