import { randomName, randomSurname } from "./constants/constants"
import useMain from "./hooks/main.hooks"


const Main = ()=>{
    const {users} = useMain()
    return <div>
        
    </div>
}
export default Main